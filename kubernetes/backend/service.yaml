---
apiVersion: v1
kind: Service
metadata:
  name: product-crud-backend
  namespace: product-crud
  labels:
    app: product-crud-backend
spec:
  type: ClusterIP
  selector:
    app: product-crud-backend
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
